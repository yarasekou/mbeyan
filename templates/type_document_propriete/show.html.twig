{% extends 'base.html.twig' %}

{% block title %}TypeDocumentPropriete{% endblock %}

{% block body %}
    <h1>TypeDocumentPropriete</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_document_propriete.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_document_propriete.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_document_propriete.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_document_propriete_index') }}">back to list</a>

    <a href="{{ path('type_document_propriete_edit', {'id': type_document_propriete.id}) }}">edit</a>

    {{ include('type_document_propriete/_delete_form.html.twig') }}
{% endblock %}
