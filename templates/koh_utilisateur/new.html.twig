{% extends 'base_setting.html.twig' %}
{% block title %}Nouvel administrateur de l'application{% endblock %}
{% block body %}
    <h5 class="font-weight-bolder text-uppercase">Enregistrement nouvel administrateur mbeyan</h5>
    <hr>
    <div class="card mt-3 mb-5">
        <h5 class="card-header bg-dark text-white font-weight-bolder text-uppercase">Nouvel administrateur mbeyan</h5>
        <div class="card-body font-weight-bolder text-uppercase">
            {{ include('koh_utilisateur/_form.html.twig') }}
        </div>
        <div class="card-footer">
            <a class="font-weight-bolder text-uppercase text-decoration-none"
               href="{{ path('koh_utilisateur_index') }}">Afficher la lsite des utilisateurs</a>
        </div>
    </div>
{% endblock %}
