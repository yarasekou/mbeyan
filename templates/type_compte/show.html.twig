{% extends 'base.html.twig' %}

{% block title %}TypeCompte{% endblock %}

{% block body %}
    <h1>TypeCompte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_compte.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_compte.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_compte.description }}</td>
            </tr>
            <tr>
                <th>HasManyUser</th>
                <td>{{ type_compte.hasManyUser ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>IsStructure</th>
                <td>{{ type_compte.isStructure ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_compte_index') }}">back to list</a>

    <a href="{{ path('type_compte_edit', {'id': type_compte.id}) }}">edit</a>

    {{ include('type_compte/_delete_form.html.twig') }}
{% endblock %}
