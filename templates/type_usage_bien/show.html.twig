{% extends 'base.html.twig' %}

{% block title %}TypeUsageBien{% endblock %}

{% block body %}
    <h1>TypeUsageBien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_usage_bien.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_usage_bien.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_usage_bien.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_usage_bien_index') }}">back to list</a>

    <a href="{{ path('type_usage_bien_edit', {'id': type_usage_bien.id}) }}">edit</a>

    {{ include('type_usage_bien/_delete_form.html.twig') }}
{% endblock %}
