{% extends 'base.html.twig' %}

{% block title %}TypeAcquisition{% endblock %}

{% block body %}
    <h1>TypeAcquisition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_acquisition.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_acquisition.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_acquisition.description }}</td>
            </tr>
            <tr>
                <th>IsBienForStructure</th>
                <td>{{ type_acquisition.isBienForStructure ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_acquisition_index') }}">back to list</a>

    <a href="{{ path('type_acquisition_edit', {'id': type_acquisition.id}) }}">edit</a>

    {{ include('type_acquisition/_delete_form.html.twig') }}
{% endblock %}
