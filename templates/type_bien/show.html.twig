{% extends 'base.html.twig' %}

{% block title %}TypeBien{% endblock %}

{% block body %}
    <h1>TypeBien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_bien.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_bien.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_bien.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_bien_index') }}">back to list</a>

    <a href="{{ path('type_bien_edit', {'id': type_bien.id}) }}">edit</a>

    {{ include('type_bien/_delete_form.html.twig') }}
{% endblock %}
